schema_version: "1.0"

name: "shallow-function-reactivity"
version: "0.1.0"
description: "Add deep: true to useLazyAsyncData, useAsyncData, useFetch, and useLazyFetch calls"
author: "Codemod Team"
license: "MIT"
workflow: "workflow.yaml"
category: "migration"

targets:
  languages: ["tsx"]

keywords: ["transformation", "migration"]

registry:
  access: "private"
  visibility: "private"

// CASE 3: REPLACE - Import statement exists but needs editing
// Expected: Should replace existing import with new one containing both resolve and join

import { resolve, join } from "node:path";

console.log("Hello World");
someFunction();

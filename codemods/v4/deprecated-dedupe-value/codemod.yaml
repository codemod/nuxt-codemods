schema_version: "1.0"

name: "deprecated-dedupe-value"
version: "0.1.0"
description: "Transform deprecated dedupe values in refresh() calls"
author: "Codemod Team"
license: "MIT"
workflow: "workflow.yaml"
category: "migration"

targets:
  languages: ["tsx"]

keywords: ["transformation", "migration"]

registry:
  access: "private"
  visibility: "private"
